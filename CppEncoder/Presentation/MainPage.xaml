<Page x:Class="CppEncoder.Presentation.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:CppEncoder.Presentation"
      xmlns:uen="using:Uno.Extensions.Navigation.UI"
      xmlns:utu="using:Uno.Toolkit.UI"
      NavigationCacheMode="Required"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="C++ UTF-8 BOM Encoder" FontSize="24" Margin="0,0,0,20" Grid.Row="0"/>
        <Button Content="Select C++ Files" Click="OnSelectFilesClick" Grid.Row="1"/>
        <ListView x:Name="FileListView"
                  Margin="0,10,0,10"
                  Grid.Row="2"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"/>
        <Button Content="Encode as UTF-8 with BOM" Click="OnEncodeClick" Grid.Row="3"/>
        <TextBlock x:Name="StatusText" Foreground="Green" Margin="0,10,0,0" Grid.Row="4"/>
    </Grid>
</Page>
